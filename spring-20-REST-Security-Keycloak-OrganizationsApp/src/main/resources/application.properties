server.port=8082
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=1213
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=create
spring.jpa.defer-datasource-initialization=true

#which realm are we working with
keycloak.realm=cydeo-dev
#where the server is running
keycloak.auth-server-url=http://localhost:8080/auth
keycloak.ssl-required=external
#the client to be authorized
keycloak.resource=training
#secret credentials of the client
keycloak.credentials.secret=Gd80nmh7TnKKSjbWiYTPHyGQ5rgZx4jH
#is there a composite role
keycloak.use-resource-role-mappings=true
keycloak.bearer-only=true
spring.main.allow-circular-references=true

